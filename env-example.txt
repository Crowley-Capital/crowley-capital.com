# ==================== DATABASE ====================
DATABASE_URL=postgresql://user:password@host:port/database

# ==================== OPENAI API ====================
OPENAI_API_KEY=your-openai-api-key-here

# ==================== AI MODEL CONFIGURATION ====================
# Content Generation Models
CONTENT_MODEL=gpt-4o
CONTENT_MODEL_FALLBACK=gpt-4o-mini

# Formatting Models
FORMATTER_MODEL=gpt-4o-mini
FORMATTER_MODEL_FALLBACK=gpt-4o-mini

# ==================== BACKEND ====================
PORT=3001
# For production on Render, use 10000 (Render's default)
# PORT=10000

FRONTEND_URL=https://your-domain.com
NODE_ENV=development

# ==================== FRONTEND ====================
# These variables are prefixed with API_ or ADMIN_ to be accessible in the frontend
# (See vite.config.ts envPrefix setting)

# For LOCAL DEVELOPMENT (separate frontend/backend):
API_URL=http://localhost:3001/api

# For PRODUCTION (single web service on Render):
# API_URL=/api

ADMIN_PASSWORD=your-admin-password-here

# ==================== CLOUD STORAGE (Required for Production) ====================
# Cloudflare R2 (S3-compatible, free tier: 10GB storage, 1M reads/month)
# Without this, images will be saved locally and lost on Render redeploys
R2_ACCOUNT_ID=your-account-id
R2_ACCESS_KEY_ID=your-access-key-id
R2_SECRET_ACCESS_KEY=your-secret-access-key
R2_BUCKET_NAME=crowley-capital-images
R2_PUBLIC_URL=https://images.your-domain.com

# Optional: Use AWS S3 instead (same SDK, different endpoint)
# Endpoint would be: https://s3.amazonaws.com

# ==================== DEPLOYMENT OPTIONS ====================
# 
# Option A: Separate Services (Static Site + Web Service)
# - Frontend: Static site on Render (free tier)
# - Backend: Web service on Render (starter plan)
# - Set API_URL=https://crowley-capital-api.onrender.com/api
#
# Option B: Single Web Service (recommended for simplicity)
# - Combined: Everything on one web service (starter plan)
# - Set API_URL=/api (relative URL)
# - Backend serves the frontend build

